language: swift
osx_image: xcode10

before_install:
  - pod repo update
  - git submodule update --init --recursive
  
script:
  - xcodebuild -scheme MobileDB -workspace MobileDB.xcworkspace -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone XS,OS=12.0' build test

after_success:
  - bash <(curl -s https://codecov.io/bash)
